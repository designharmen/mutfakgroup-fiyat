package com.harmen.pafta

import android.app.Application

/**
 * The application object.
 *
 * It holds nothing yet; it exists so that the project store and the renderer's
 * native engine have a single place to be created once, rather than being bolted
 * onto the activity when those phases land.
 */
public class PaftaApplication : Application()
